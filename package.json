{
  "name": "subgraphdemo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraphdemo",
    "create-local": "graph create --node http://localhost:8020/ subgraphdemo",
    "remove-local": "graph remove --node http://localhost:8020/ subgraphdemo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraphdemo"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
